<%= form_with(model: [@forum, discussion]) do |f| %>
  <% if discussion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
        <% discussion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="mb-3">
    <%#= f.select :forum_id, Forum.all.pluck(:name, :id), { include_blank: 'Select forum' }, placeholder: 'Forum name' %>
  </div>-->

  <div>
    <%= f.label :title, style: "display: block" %>
    <%= f.text_field :title, class: "form-control" %>
  </div>

  <div>
    <%= f.label :body, style: "display: block" %>
    <%= f.rich_text_area :body %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>
